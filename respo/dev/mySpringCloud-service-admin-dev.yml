spring:
  application:
    name: mySpringCloud-service-admin
  datasource:
    druid:
      url: jdbc:mysql://localhost:3306/miyi-meishi-service-admin?useUnicode=true&characterEncoding=utf-8&useSSL=false
      username: root
      password: cl1991625
      initial-size: 1
      min-idle: 1
      max-active: 20
      test-on-borrow: true
      driver-class-name: com.mysql.jdbc.Driver

server:
  port: 8900
